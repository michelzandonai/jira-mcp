{
  "mcpServers": {
    "jira-agent": {
      "command": "docker",
      "args": [
        "exec",
        "-i",
        "jira-mcp-server",
        "python",
        "mcp_server.py"
      ],
      "env": {
        "DOCKER_HOST": "unix:///var/run/docker.sock"
      }
    }
  }
}